{% macro renderSlides(elementsByColumn) %}
    {% for column in elementsByColumn %}
            <div class="{{ column }}">
                {% if elementsByBlock[column] is defined %}
                    {% for element in elementsByBlock[id] %}
                        {{ element|raw }}
                    {% endfor %}
                {% endif %}
                
            </div>
      
    {% endfor %}
{% endmacro %}
{% import _self as layout %}

<div id="{{ cssId }}" class="{{ cssClass }}" >

<{{ headlineTag }}>{{ headline }}</{{ headlineTag }}>
<div class="content-slider" data-config="{{ settings.delay }},{{ settings.speed }},{{ settings.startSlide }},{{ settings.continuous }}">
    <div class="slider-wrapper">
        {{ layout.renderSlides(elementsByColumn) }}

      </div>
  </div>

  <nav class="slider-control" aria-label="Slider">
    <a href="#" class="slider-prev"> {{ previous }}</a>
    <span class="slider-menu"></span>
    <a href="#" class="slider-next">{{ next }}</a>
  </nav>
</div>